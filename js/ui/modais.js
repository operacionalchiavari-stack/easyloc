/**
 * MODAL SYSTEM MODULE — UI Dialog Management
 * 
 * Exports:
 * - window.abrirPerfil()
 * - window.abrirEmpresa()
 */

window.abrirPerfil = function () {
  criarModalPerfil();
};

window.abrirEmpresa = function () {
  criarModalEmpresa();
};
